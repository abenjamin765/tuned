import { trioImagePartA } from '@/lib/img-trio-a';
import { trioImagePartB } from '@/lib/img-trio-b';
import { trioImagePartC } from '@/lib/img-trio-c';
import { trioImagePartD } from '@/lib/img-trio-d';
import { trioImagePartE } from '@/lib/img-trio-e';
import { trioImagePartF } from '@/lib/img-trio-f';
import { trioImagePartG } from '@/lib/img-trio-g';
import { trioImagePartH } from '@/lib/img-trio-h';
export const trioImage = 'data:image/jpeg;base64,' + trioImagePartA + trioImagePartB + trioImagePartC + trioImagePartD + trioImagePartE + trioImagePartF + trioImagePartG + trioImagePartH;
